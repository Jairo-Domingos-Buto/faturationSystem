const apiurl = 'api/clientes'


const selecionarUsuario = document.getElementById('selecionarUsuario')

const modal = document.getElementById(
  'modal'
)

selecionarUsuario.addEventListener('click', () => {

})
